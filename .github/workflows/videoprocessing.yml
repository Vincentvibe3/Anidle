name: Process Video

on:
  schedule:
    # * is a special character in YAML so you have to quote this string
    - cron:  '0 11 * * *'
  workflow_dispatch:
  push:
    branches:
        - main
    paths:
        - 'songs.json'
        - 'src/lib/songs.ts'
    

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [16.x]
        # See supported Node.js release schedule at https://nodejs.org/en/about/releases/
    defaults:
      run:
        working-directory: ./videoProcessing
        
    steps:
    - uses: actions/checkout@v3
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
    
    - name: Download ffmpeg
      run: curl -L --output ffmpeg.tar.xz https://johnvansickle.com/ffmpeg/builds/ffmpeg-git-amd64-static.tar.xz
      
    - name: Create ffmpeg directory
      run: mkdir ffmpeg
      
    - name: Unzip ffmpeg
      run: tar -xvf ffmpeg.tar.xz -C ffmpeg --strip-components=1
      
    - name: Add ffmpeg to PATH
      run: echo "$GITHUB_WORKSPACE/videoProcessing/ffmpeg" >> $GITHUB_PATH
      
    - name: Install npm dependencies
      run: npm install
      
    - name: Allow running video processing script
      run: chmod +x ./getKeyframes.sh
      
    - name: Run downloader and script
      run: node downloader.js ../songs.json
      
    - name: Process data and upload
      run : node process.js
      env: 
        SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        EMAIL: ${{ secrets.EMAIL }}
        PASSWORD: ${{ secrets.PASSWORD }}
        SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
        BUCKET_NAME: ${{ secrets.BUCKET_NAME }}
